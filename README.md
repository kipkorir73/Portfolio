# Collins Kipkorir

Two sites live in this project:

- **Portfolio** (this folder): static site. `python3 -m http.server 43123`
- **Apply Desk** (`apply-desk/`): scan matching jobs, apply yourself via the link, log it. Gmail follow-up later. `cd apply-desk && npm run dev` → http://127.0.0.1:43124
